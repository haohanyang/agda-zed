(function
  (lhs
    (function_name
      (atom
        (qid) @name)))
  (rhs (expr) @context)
) @item

(module
  (module_name) @name
) @item

(postulate
  "postulate" @name
) @item

(data_signature
  (data_name) @name
  (expr) @context
) @item

(data
  (data_name) @name
  (expr) @context
) @item

(data
  (data_name) @name
) @item

(record_signature
  (record_name) @name
  (expr) @context
) @item

(record
  (record_name) @name
  (expr) @context
) @item

(record
  (record_name) @name
) @item
